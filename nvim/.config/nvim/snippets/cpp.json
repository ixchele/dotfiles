{
  "Main Function": {
    "prefix": "main",
    "body": [
      "int main()",
      "{",
      "\t$0",
      "\treturn 0;",
      "}"
    ],
    "description": "Boilerplate for main function"
  },
  "Iterateur standard": {
    "prefix": "forit",
    "body": [
      "for (${1:type}::iterator it = ${2:container}.begin(); it != ${2:container}.end(); ++it) {",
      "\t$0",
      "}"
    ],
    "description": "for loop with iterator"
  }
}


